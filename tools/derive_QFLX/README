
`make_forcing_from_hist.jl` makes QFLX based on WKSTT and WKSTS (weak storing terms).
`plot_OHT.jl` plots ocean heat transport based on ADVT and WKSTT.
`plot_OHT_QFLXT.jl` plots ocean heat transport based on QFLXT (I use it specifically to QFLX forcing files).
